<br><br>
<form method="post" action="{{ path('back_deleteconstat', {'id': constat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ constat.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
